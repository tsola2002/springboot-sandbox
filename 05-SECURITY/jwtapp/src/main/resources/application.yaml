spring:
  application:
    name: jwtapp
  datasource:
    url: jdbc:mysql://localhost:3306/jwtdb
    username: root
    password:
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
